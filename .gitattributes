# General rule first - later rules override earlier ones.
* text=auto

# Batch files must keep CRLF. With LF, cmd.exe fails with "The system cannot
# find the batch label specified" on any goto. -text stops git normalising
# them, so the bytes in the repo are the bytes GitHub serves in the ZIP.
*.bat -text
